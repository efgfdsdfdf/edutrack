{
  "name": "ace",
  "version": "1.0.0",
  "description": "ACE app frontend",
  "main": "index.html",
  "scripts": {
    "start": "live-server",
    "build": "echo 'No build step, static files'"
  },
  "dependencies": {
    "@capacitor/core": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0",
    "live-server": "^1.2.1"
  }
}
